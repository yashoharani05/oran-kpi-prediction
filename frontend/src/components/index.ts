/*
  components/index.ts
  Barrel export file for reusable UI components.

  Components will be added here in Phase 3 when the dashboard is built.
  Planned components:
    - <StatusBadge />      — shows "Normal" (green) or "Degraded" (red)
    - <MetricCard />       — displays a single KPI value with label
    - <ThroughputChart />  — line chart for LSTM forecast
    - <FeatureImportance/> — bar chart showing which KPIs matter most
    - <ModelComparison />  — table comparing RF, XGBoost, LSTM metrics
*/

// export { StatusBadge } from "./StatusBadge";
// export { MetricCard } from "./MetricCard";
// (Uncomment as components are created in Phase 3)
